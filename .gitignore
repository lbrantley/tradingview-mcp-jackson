node_modules/
screenshots/
scripts/current.pine
temp_*
*.log
discovery-log.json

# Secrets — never commit
.env
.env.*
!.env.example

# Install-time symlink created by the one-shot setup
AGENTS.md

# Scanner runtime state (regenerated each run / accumulates per scan)
news_cache.json
scanner_audit.json
scanner_audit_pre_v3_backup.json
strength_cache.json
notification_cache.json
